# Unix
alias ln="ln -v"
alias mkdir="mkdir -p"
alias e="$EDITOR"
alias v="$VISUAL"


alias psa="ps aux"

# homebrew - Mac
alias bru="brew update && brew upgrade && brew cleanup && brew doctor"
alias bcu="brew cu"

# vim mimic
alias :q="exit"
alias :qq="rm -Rfr ~/.zsh_history && :q"

# vimrc editing
alias ve='vim ~/.vimrc'

# zsh profile editing
alias ze='vim ~/.zshrc'

# zsh shell reset
alias sz='source ~/.zshrc'

# shortcuts
alias rsl='defaults write com.apple.dock ResetLaunchPad -bool true && killall Dock'
alias ls='exa'

#flushdns
alias flushdns='sudo killall -HUP mDNSResponder'

#archLinux
alias pkgclr='yay -Sc'
alias orphan='yay -Rnsc $(trizen -Qqddtt)'
alias mirrorsup='sudo reflector --verbose --country Netherlands --country France --country Germany --country India --country US --age 12 --latest 10 --protocol https --sort rate --save /etc/pacman.d/mirrorlist'

alias static='P=( " " █ ░ ▒ ▓ );while :;do printf "\e[$[RANDOM%LINES+1];$[RANDOM%COLUMNS+1]f${P[$RANDOM%5]}";done|lolcat'

##VoidLinux
alias xs='sudo xbps-install'
alias xr='sudo xbps-remove'
alias xrr='sudo xbps-remove -R'
alias xrrf='sudo xbps-remove -RF'
alias xq='sudo xbps-query'
